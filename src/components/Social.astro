---
const { platform, username, extension, name } = Astro.props;
---

<a href={`https://www.${platform}${extension ? extension : ".com"}/${username}`}>{name ? name : platform}</a>

<style>
	a {
		padding: 0.5rem 1rem;
		color: white;
		background-color: #4c1d95;
		text-decoration: none;
	}
</style>
